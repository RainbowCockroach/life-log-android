<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Temp files the camera app writes captured photos into, before we import + downscale them. -->
    <files-path name="camera_temp" path="camera_temp/" />
</paths>
